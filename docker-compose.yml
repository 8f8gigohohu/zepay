services:
  zepay:
    build: .
    image: zepay:ultimate
    container_name: zepay
    ports:
      - "8000:8000"
    volumes:
      - zepay_data:/data
    environment:
      ZEPAY_DATA_DIR: /data
      ZEPAY_PORT: "8000"
    restart: unless-stopped

volumes:
  zepay_data:
